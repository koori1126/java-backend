# このファイルをコピーして ".env" という名前で使ってください(.envはgit管理対象外)。
# デフォルト値は開発用PostgreSQL(192.168.20.246)に接続する前提になっています。
# パスワードだけは既定値を持たせていないため、.envで必ず指定してください。

DB_HOST=192.168.20.246
DB_PORT=5432
DB_NAME=db01
DB_USERNAME=dbadmin
DB_PASSWORD=

# ==== 別の接続先(開発環境のEDB等)に切り替える場合は、上記を書き換える ====
